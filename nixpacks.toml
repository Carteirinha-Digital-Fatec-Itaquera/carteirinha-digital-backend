[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm install --legacy-peer-deps && npx prisma generate && npx nest build"]

[start]
cmd = "node dist/src/main.js"
