[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --ignore-engines"]

[phases.build]
cmds = ["npx prisma generate && npx nest build"]

[start]
cmd = "node dist/src/main.js"
