{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && npx prisma generate && npx nest build"
  },
  "deploy": {
    "startCommand": "node dist/src/main.js"
  }
}
